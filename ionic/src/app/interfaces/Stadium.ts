export interface Stadium {
    _id?: string,
    belongToSede?: string,
    code: string,
    type: number,
    length: number,
    width: number,
    roof: string,
    grass: string,
    punctuaction: number,
}